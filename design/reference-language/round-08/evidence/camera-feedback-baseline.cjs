const {chromium}=require('../../../../todo/node_modules/@playwright/test');
const fs=require('node:fs');
(async()=>{const b=await chromium.launch({channel:'chrome'});const p=await b.newPage({viewport:{width:390,height:844}});await p.route('**/assets/image.svg',route=>route.abort());await p.goto('http://127.0.0.1:4198');await p.getByRole('button',{name:'내 냉장고',exact:true}).click();await p.getByRole('button',{name:'사진으로 재료 등록'}).click();await p.screenshot({path:__dirname+'/camera-feedback-before.png'});const result=await p.locator('[data-action=upload]').evaluate(el=>{const node=[...el.childNodes].find(n=>n.nodeType===Node.TEXT_NODE);const range=document.createRange();range.selectNodeContents(node);const t=range.getBoundingClientRect(),box=el.getBoundingClientRect();return {gap:getComputedStyle(el).gap,textCenter:t.x+t.width/2,buttonCenter:box.x+box.width/2,iconSlotWidth:el.querySelector('.icon')?.getBoundingClientRect().width};});fs.writeFileSync(__dirname+'/camera-feedback-before.json',JSON.stringify(result,null,2));console.log(result);await b.close();})()
